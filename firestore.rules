// Merge this block into your existing Firestore rules in the Firebase console
// (or your deployed rules file). Required for appointment/reschedule emails.
//
// match /outbound_mail/{mailId} {
//   allow create: if request.auth != null
//     && request.resource.data.status == 'pending'
//     && request.resource.data.to is string
//     && request.resource.data.to.size() > 3
//     && request.resource.data.subject is string
//     && request.resource.data.html is string;
//   allow read: if request.auth != null;
//   allow update, delete: if false;
// }
