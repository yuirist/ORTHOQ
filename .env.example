# Copy to `.env` and add your keys.
# Alternatively pass at build time: flutter run --dart-define=GEMINI_API_KEY=your_key_here
GEMINI_API_KEY=

# Gmail SMTP for transactional email (used by Cloud Functions; optional client fallback on mobile/desktop).
SMTP_USER=
SMTP_APP_PASSWORD=
